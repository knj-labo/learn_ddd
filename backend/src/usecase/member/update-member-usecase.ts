import { UseCase } from '../../utils/usecase';

export class UpdateMemberUsecase implements UseCase<any, Promise<any>> {
  constructor() {}
  public async execute(): Promise<any> {
    // TODO: implement use case
  }
}
